const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/workforge').then(async () => { const Company = mongoose.model('Company', new mongoose.Schema({}, { strict: false })); const Employee = mongoose.model('Employee', new mongoose.Schema({}, { strict: false })); const emps = await Employee.find({}); const emails = emps.map(e => e.email); const res = await Company.updateMany({ email: { $in: emails } }, { $set: { isTeam: true } }); console.log(res); process.exit(); });
